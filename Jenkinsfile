pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo 'Building..'
        // add real build commands here, e.g. sh 'mvn -B package'
      }
    }
    stage('Test') {
      steps {
        echo 'Testing..'
        // add test commands here
      }
    }
    stage('Deploy') {
      steps {
        echo 'Deploying....'
        // add deploy commands here
      }
    }
  }
}
